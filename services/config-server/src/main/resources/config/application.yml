spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
    
server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health: 
    configserver:
      enabled: false